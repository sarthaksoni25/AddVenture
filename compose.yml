services:
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile.backend
    ports:
      - "50001:5001"  # Host:Container
    volumes:
      - ./server/adventure.db:/app/adventure.db
    environment:
      - PORT=5001
